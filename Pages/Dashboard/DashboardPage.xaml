<Page x:Class="StoDamageMeter.Pages.Dashboard.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:shared="clr-namespace:StoDamageMeter.Components.Shared"
    xmlns:dashboard="clr-namespace:StoDamageMeter.Components.PageSpecific.Dashboard"
    Title="Dashboard">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/ModernStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
        HorizontalScrollBarVisibility="Disabled"
        Padding="24">
        <StackPanel>

            <!-- Quick Actions Grid -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- File Selection Card -->
                <dashboard:FileSelectionCard Grid.Column="0"
                    Margin="0,0,12,0" />

            </Grid>

            <!-- Loading Spinner -->
            <shared:LoadingSpinner Margin="0,0,0,24" />

            <!-- Combatlog Info Card -->
            <shared:CombatLogInfoCard Margin="0,0,0,24" />

        </StackPanel>
    </ScrollViewer>
</Page>