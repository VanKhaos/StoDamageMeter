<UserControl x:Class="StoDamageMeter.Components.Shared.LogFileSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="800">
    <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="16">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Combat Log:"
                       VerticalAlignment="Center"
                       Foreground="#E0E0E0"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,12,0"/>

            <TextBox x:Name="LogPathTextBox"
                     Grid.Column="1"
                     IsReadOnly="True"
                     VerticalAlignment="Center"
                     Background="#3C3C3C"
                     Foreground="#E0E0E0"
                     BorderBrush="#555555"
                     BorderThickness="1"
                     Padding="8,6"
                     FontSize="13"
                     Margin="0,0,8,0"/>

            <Button x:Name="BrowseButton"
                    Grid.Column="2"
                    Content="Browse..."
                    Click="OnBrowseClick"
                    Padding="16,6"
                    FontSize="13"
                    Background="#0E639C"
                    Foreground="White"
                    BorderBrush="#007ACC"
                    BorderThickness="1"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1177BB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D5A8F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>













